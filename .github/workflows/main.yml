name: Generate Snake Animation

on:
  # executa automaticamente 1x por dia
  schedule:
    - cron: "0 0 * * *"
  
  # permite rodar manualmente
  workflow_dispatch:
  
  # roda em cada push no branch principal
  push:
    branches:
      - main  # ou "master" se for esse o nome no seu repositório

jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      - name: Gerar os SVGs da cobrinha
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          # Dois jogos de nomes: github-snake.* e o nome legado
          # github-contribution-grid-snake.*, porque geradores de perfil
          # (ex.: GitAscii) procuram por uma convencao ou pela outra.
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
          
      - name: Publicar no branch output
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
